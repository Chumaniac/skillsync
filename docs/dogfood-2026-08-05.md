# Local Skill Dogfood Report

**Run date:** 2026-08-05
**Mode:** Read-only local scan and verification
**Command surface:** compiled `skillsync scan` and `skillsync verify`

## Targets

- `~/.agents/skills`
- `~/.claude/skills`

The commands only read Skill files and emit bounded findings. They do not
modify either target directory or execute Skill instructions.

## Results

The scan discovered six Skill instances. Two names exist in both targets and
have different content digests:

- `genkoy-component-splitter`
- `genkoy-store-refactor`

The Codex-targeted verification report contained:

| Result | Count |
| --- | ---: |
| Pass | 4 |
| Warn | 8 |
| Fail | 4 |
| Unknown | 0 |

The four failures were invalid or missing YAML frontmatter in:

- `brand-voice`
- `genkoy-component-splitter` under `.claude/skills`
- `genkoy-store-refactor` under `.claude/skills`
- `product-lens`

Warnings reported local-only provenance for the discovered Skills and drift
between the duplicate copies. The verification command therefore exited with
code `1`, as intended when structural errors are present.

## Follow-up outside SkillSync

The target directories are user-managed inputs, so this project does not
rewrite them automatically. A future adoption/reconciliation workflow can
address the findings explicitly by:

1. adding valid frontmatter to the four affected Skill documents;
2. choosing a canonical copy for each duplicated Skill and re-scanning; and
3. recording repository and resolved-commit provenance where those Skills are
   published.
