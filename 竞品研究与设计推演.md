# SkillSync 竞品研究与设计推演

> 研究日期：2026-08-04
> 目的：记录产品定位调整的事实依据、替代方案与关键决策
> 说明：GitHub Stars、Issue 数和功能状态会变化；这里记录的是本次研究时看到的公开页面快照。

---

## 1. 原设计的哪些判断需要推翻

原文把以下能力视为主要空白：

```text
diff / doctor / rollback / lockfile
```

这套判断不再准确。

### 1.1 `runkids/skillshare`

项目 README 已经将自己定位为多 Agent 的 source-of-truth 同步工具，覆盖 Claude、Cursor、Codex、OpenClaw、OpenCode 和更多目标；同时具备 security audit、项目模式、GitHub Actions、UI、过滤和本地 checkpoint。

官方文档还列出：

- `skillshare diff`：目标级、文件级和 unified patch 差异；
- `skillshare doctor`：目标状态、broken symlink、Skill 完整性、SHA-256 文件哈希、重复路径和 CI JSON；
- `skillshare backup/restore`：自动备份、按时间点恢复、恢复前对比；
- `skillshare audit`：对 prompt injection 和数据外泄进行扫描。

来源：

- [GitHub README](https://github.com/runkids/skillshare)
- [Commands](https://skillshare.runkids.cc/docs/reference/commands/)
- [doctor](https://skillshare.runkids.cc/docs/reference/commands/doctor/)
- [diff](https://skillshare.runkids.cc/docs/reference/commands/diff/)
- [backup](https://skillshare.runkids.cc/docs/reference/commands/backup/)
- [restore](https://skillshare.runkids.cc/docs/reference/commands/restore/)

结论：不能再声称“没有人做 diff/doctor/rollback”。

### 1.2 `xingkongliang/skills-manager`

它已经不是“只有 GUI”的轻量管理器。README 当前明确包含：

- 中央 Skill library；
- Git、local folder、zip、`.skill` 和 skills.sh 安装；
- presets、global/project workspace、linked workspace；
- 上游更新检查和本地/上游内容比较；
- agent-friendly CLI；
- GitHub 私有仓库备份；
- snapshot restore；
- skill-aware merge 和冲突解决；
- activity log 和导出日志。

来源：[skills-manager README](https://github.com/xingkongliang/skills-manager)

结论：GUI、备份、快照、CLI、跨设备同步都不是独特卖点。

### 1.3 `vercel-labs/skills`

`npx skills` 已经提供：

- `add`、`use`、`list`、`find`、`remove`、`update`、`init`；
- project/global scope；
- 多 Agent 安装；
- symlink/copy 两种方式；
- GitHub/GitLab/本地路径和归档源；
- 70+ Agent target。

来源：[vercel-labs/skills README](https://github.com/vercel-labs/skills)

它的 Issue #283 反而暴露了一个更精准的缺口：现有 `~/.agents/.skill-lock.json` 包含 source、path、hash、时间戳，但仍更像更新追踪数据库，不是可以在新机器上完整恢复所有 Skill 的声明式安装清单。

来源：[Issue #283](https://github.com/vercel-labs/skills/issues/283)

结论：lockfile 仍有机会，但不能只做“把字段换成另一种 JSON”。真正要做的是“可复现分发 + target compatibility + verification evidence”。

### 1.4 GitHub 官方 `gh skill`

GitHub CLI 当前官方手册已经列出 `gh skill install/list/preview/publish/search/update`。`publish --dry-run` 会检查 Skill 名称、目录名、frontmatter、`allowed-tools` 类型和发布元数据。

来源：

- [`gh skill` 手册](https://cli.github.com/manual/gh_skill)
- [`gh skill publish` 手册](https://cli.github.com/manual/gh_skill_publish)
- [GitHub CLI 仓库](https://github.com/cli/cli)

结论：Skill 的安装和基础发布校验正在进入 GitHub 官方工具链，SkillSync 不应与其争夺入口。

### 1.5 `rulesync` 与 Agent Skills 标准

`rulesync` 已覆盖 rules、ignore、MCP、commands、subagents、skills、hooks、permissions 等配置的导入、生成和转换。

来源：[rulesync](https://github.com/dyoshikawa/rulesync)

`agentskills/agentskills` 作为标准仓库，已经将 Skill 定义为包含 `SKILL.md`、脚本、参考资料和资源的目录，并强调渐进式披露和跨产品复用。

来源：[Agent Skills 标准仓库](https://github.com/agentskills/agentskills)

社区 Discussion #210 进一步提出分发层 `skills.json` 与 `skills.lock`，将来源、依赖、版本和 integrity 与 Agent 运行时读取的 `SKILL.md` 分离。

来源：[Skill Package Manifest RFC](https://github.com/agentskills/agentskills/discussions/210)

结论：SkillSync 应该成为分发层和验证层的实验场，同时跟随标准，而不是独立创造一个闭环格式。

### 1.6 安全扫描已经是独立赛道

安全不再是简单的“frontmatter doctor”。

- [Cisco Skill Scanner](https://github.com/cisco-ai-defense/skill-scanner) 覆盖 prompt injection、数据外泄、恶意代码模式、YAML/YARA、LLM 分析、行为数据流、SARIF 和 pre-commit。
- [SkillFortify](https://github.com/qualixar/skillfortify) 关注形式化分析、能力验证、依赖图、SBOM 和 deterministic lockfile。

结论：SkillSync 可以把安全作为 L3 证据层和策略入口，但不应把“自己写一个扫描器”当作全部产品。

---

## 2. 三种产品方向

### 方案 A：兼容性与质量门禁（推荐）

**核心**：`scan → verify → compat → diff → CI`

优势：

- 与所有现有安装器和同步器互补；
- 用户可以对现有目录直接运行，不需要迁移；
- 对 Skill 作者和团队有持续的 PR/CI 使用场景；
- 核心检查确定性强，不依赖 AI API；
- 可以从三个 Agent profile 起步，不必追逐“支持 60+ Agent”。

缺点：

- 初次体验不如桌面管理器直观；
- 需要把“语义兼容性”解释得足够清楚；
- 行为契约测试需要后续建设 fixture 和 sandbox。

### 方案 B：可复现 Skill Package Manager

**核心**：manifest、lockfile、install、reconcile、restore。

优势：

- 技术边界容易理解；
- 团队用户有明确价值；
- 可以对齐 `skills.json` / `skills.lock` 的社区 RFC。

缺点：

- 与 `npx skills`、skillshare、Skills Manager 高度重叠；
- GitHub 官方工具和竞品都有可能快速补上；
- 如果只有复制和回滚，Star 动机不足。

决策：作为 P1 的分发层能力，而不是产品首屏。

### 方案 C：Skill Supply Chain Security

**核心**：静态分析、能力声明、隔离执行、SBOM、签名和信任策略。

优势：

- Skill 会影响 Agent 的文件、终端和网络行为，安全需求真实；
- 企业和安全团队有明确预算；
- 证据和报告容易接入 CI。

缺点：

- Cisco、SkillFortify 等已有专业竞争者；
- 误报/漏报会直接伤害信任；
- “扫描通过”不能等价于安全，需要较强 threat model。

决策：作为 L3 适配器和 policy 层，不作为早期唯一定位。

---

## 3. 推荐组合

```text
主产品：方案 A，兼容性与质量验证
底层能力：方案 B，来源与可复现 lock
安全能力：方案 C，静态证据与外部扫描适配器
```

最终叙事：

> 现有工具帮助你找到、安装、同步 Skill；SkillSync 帮你证明变更没有偷偷改变它的有效行为。

---

## 4. 真正的差异化是什么

### 4.1 从文件 Diff 到语义 Diff

普通文件 Diff 只能说：

```diff
- description: review code
+ description: review code and push changes
```

SkillSync 应该说：

```text
routing-change
description expands from review-only to review-and-push
risk: activation scope increased

capability-change
new shell script added: scripts/push.sh
risk: write/network capability requires review
```

### 4.2 从路径兼容到能力兼容

路径同步只知道 `~/.claude/skills` 是否存在。

能力验证要知道：

- target 是否识别该 frontmatter 字段；
- `allowed-tools` 是否真正被强制；
- `context: fork` 是否有等价执行模型；
- hooks 是否会运行；
- bundled scripts 的相对路径是否保持；
- unknown field 是被忽略、报错还是透传。

### 4.3 从“有备份”到“有验证证据的恢复点”

普通备份回答：“之前的文件是什么”。

SkillSync 要回答：“哪个版本通过了哪些 profile、哪些 policy、哪些行为测试”。

因此 rollback 的目标应是：

```text
last verified commit
```

而不是单纯的：

```text
latest backup directory
```

### 4.4 从“安全扫描通过”到“透明风险证据”

输出：

- 发现了什么；
- 使用了什么规则或扫描器；
- 哪些能力未被验证；
- 哪些结果需要人工判断；
- 如何复现和修复。

避免输出：

```text
This skill is safe.
```

---

## 5. 为什么这会有 Star 冲动

### 5.1 第一次运行就有结果

“先扫描，不写文件，不联网”是最低摩擦入口。

用户不需要：

- 先建立中央库；
- 先迁移目录；
- 先登录 GitHub；
- 先选择一个同步器；
- 先配置模型 API Key。

### 5.2 结果适合公开分享

SkillSync 报告天然适合：

- PR 评论；
- SARIF Code Scanning；
- README badge；
- 兼容性矩阵；
- Issue 中的最小复现。

### 5.3 社区可以贡献证据

一个 profile 或 fixture 就是一项独立贡献，不必理解整个 CLI：

```text
新增 profile
新增官方文档链接
新增一个支持/不支持 fixture
新增一条语义 diff 规则
```

这比“支持第 47 个 Agent”更可审查，也更容易形成长期资产。

### 5.4 生态越成熟，需求越强

如果未来所有 Agent 都统一使用 Agent Skills 标准，目录同步会变得不重要，但以下问题仍然存在：

- Skill 是否可信；
- 依赖是否可复现；
- target 是否完整支持；
- 变更是否改变行为；
- 团队是否批准；
- 失败能否恢复。

这使定位不依赖当前某个目录约定。

---

## 6. 关键设计取舍

### 6.1 为什么先不做 GUI

skillshare 和 Skills Manager 已经证明 GUI 是合理方向，但早期 SkillSync 的核心价值是验证证据，不是卡片管理。

CLI/CI 能让：

- 作者在提交时运行；
- 团队在 PR 中阻断；
- 个人用户在任何目录快速试用；
- 贡献者用 fixture 测试规则。

GUI 可以在验证模型稳定后消费 JSON/SARIF，而不是先反向决定领域模型。

### 6.2 为什么先不做 60+ Agent

目标数量是营销指标，不是兼容性质量指标。

三组有文档和 fixture 的 profile，比 60 个只有路径映射的 profile 更可信。

### 6.3 为什么不把 AI 放进核心判定

AI 可用于：

- 风险摘要；
- 语义变化解释；
- 可选行为测试；
- 生成修复建议。

AI 不应决定：

- 文件哈希是否一致；
- 引用文件是否存在；
- 退出码；
- policy 是否阻断；
- lockfile 是否可复现。

### 6.4 为什么不直接复制 `skillshare diff`

SkillSync 的 diff 要解释“有效行为变化”，并将变化关联到 target profile、policy 和 provenance。文件层 diff 可以作为证据，但不是最终结论。

### 6.5 为什么先用 TypeScript

Go 单二进制和 Rust 安全边界都很有吸引力，但 skill 生态当前大量使用 npm、`npx` 和 TypeScript。MVP 使用 TypeScript 可以：

- 更快与 `npx skills` lock 数据互操作；
- 降低 Skill 作者贡献门槛；
- 先验证领域模型；
- 后续再提供预构建二进制，不改变 CLI 契约。

---

## 7. 需要持续验证的假设

### H1：用户会运行只读扫描

验证方式：

- README 首屏只有一个命令；
- 不要求初始化；
- 在真实 `.claude/skills`、`.agents/skills` 和 `.cursor/skills` 上测试；
- 记录从安装到报告的时间。

### H2：Skill 作者愿意把 verify 接入 PR

验证方式：

- 提供一条 GitHub Action；
- 产生 SARIF 和短 PR 摘要；
- 为常见错误给出可复制修复命令；
- 用一个真实 Skill 仓库 dogfood。

### H3：语义兼容性比目录同步更有价值

验证方式：

- 收集 10 个真实跨 Agent 兼容性案例；
- 每个案例都能被 profile/fixture 复现；
- 若无法找到真实案例，降低语义层范围，不用概念包装代替证据。

### H4：行为契约值得后续投入

验证方式：

- 先不用真实模型，验证 fixture 和文件/工具不变量；
- 再接入一个明确隔离的 Agent sandbox；
- 只保留能稳定复现的行为断言。

---

## 8. 最终决策

```text
名称：可以暂时保留 SkillSync，但 tagline 必须说明“verify”，避免被理解成普通同步器。
主入口：npx skillsync scan
主价值：兼容性、来源、语义 Diff、CI 证据
主用户：Skill 作者 + 多 Agent 团队维护者
MVP：只读扫描、verify、compat、semantic diff、policy、SARIF
P1：lock、adopt、reconcile、可复现恢复
P2：行为契约和 sandbox
不做：市场、桌面管理、AI 生成、未经验证的多 Agent 转换
```

最终 Pitch：

> AI 能写 Skill；SkillSync 让 Skill 的变化变得可见、可验证、可复现。
